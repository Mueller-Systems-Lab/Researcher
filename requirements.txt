# =============================================================================
# Researcher — Abhängigkeiten (requirements.txt)
# =============================================================================
# Python >=3.11 erforderlich (siehe blueprint.md / gpt-researcher)
#
# Installation:
#   python -m venv venv && source venv/bin/activate
#   pip install -r requirements.txt
# =============================================================================

# ---------------------------------------------------------------------------
# Core: GPT Researcher (Fork-Basis)
# ---------------------------------------------------------------------------
# GPT Researcher is provided via git submodule `gpt_researcher/`, NOT from PyPI.
# Run before first install: git submodule update --init --recursive
# The submodule is importable as `from gpt_researcher import GPTResearcher`
# after adding the repo root to sys.path.
# gpt-researcher==0.14.8  # DO NOT install from PyPI — use the git submodule

# ---------------------------------------------------------------------------
# Darknet-Crawler & Index
# ---------------------------------------------------------------------------
# Tor SOCKS5-Proxy-Support für requests (Darknet-Crawler)
# Stand 2026-05: requests 2.34.2 ist aktuellster Patchstand
requests[socks]>=2.34.2

# HTTP/API runtime used by the MCP server and dashboard tests
fastapi>=0.115,<1.0
mcp>=1.13,<2.0

# HTML extraction used by the SSRF-protected web fetch tool
beautifulsoup4>=4.12,<5.0

# Volltext-Suchindex (Pure-Python, dateibasiert, kein Server nötig)
# DEPRECATED: unmaintained since 2021 — migrate to SQLite FTS5 (see ADR-013)
whoosh>=2.7.4

# ---------------------------------------------------------------------------
# Vektordatenbank (Lernfähigkeit)
# ---------------------------------------------------------------------------
# ChromaDB für Embedding-Persistenz (lokale Vektordatenbank)
# Exakter Pin auf 1.5.9: getestete stabile Version mit PersistentClient,
# keine Breaking Changes zu 0.5.x, Apache-2.0 Lizenz, aktiv maintained.
# Lockfile-Release: https://pypi.org/project/chromadb/1.5.9/
chromadb==1.5.9

# ---------------------------------------------------------------------------
# Sicherheitsfixes für transitive Dependencies
# ---------------------------------------------------------------------------
# lxml >=6.1.0 erforderlich wegen CVE-2026-41066 (XXE)
# Wird normalerweise transitiv über gpt-researcher installiert,
# hier explizit gepinnt für Sicherheit.
lxml>=6.1.0

# defusedxml: Schutz vor XML External Entity (XXE) Attacks
# Verwendet in scripts/classify-errors.py als Fallback für CI-JUnit-XML-Parsing
defusedxml>=0.7.1

# ---------------------------------------------------------------------------
# Dev & Testing (optional, für CI/Qualitätssicherung)
# ---------------------------------------------------------------------------
# pytest>=8.0.0
# pytest-asyncio>=0.24.0
# pytest-cov>=5.0.0
bandit>=1.7.0
