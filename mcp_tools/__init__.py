# Researcher — MCP Tools (T-016)
# Implementiert 5 MCP-Tools für das Research-System:
#   web_fetch.WebFetchTool        — Webseiten-Inhalte fetchen
#   evidence_store.EvidenceStore  — Claims in ChromaDB speichern/suchen
#   claim_validator.ClaimValidator— Claims gegen Evidenz validieren
#   audit_log.AuditLog            — Append-only Audit-Trail
#   human_review.HumanReviewTool  — Human Review Queue via MCP
